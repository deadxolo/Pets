{
  "name": "pets-fullstack",
  "version": "1.0.0",
  "description": "Full-stack Pet Services Application",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"FRONTEND,BACKEND\" --prefix-colors \"dim,bold\" \"npm run dev --prefix frontend -- --clearScreen false\" \"npm run dev --prefix backend\"",
    "start:prod": "cd backend && NODE_ENV=production npm start",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:start": "npm run build && npm run start:prod"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
